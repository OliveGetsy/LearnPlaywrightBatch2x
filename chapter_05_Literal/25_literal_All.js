let count = 42;
let negativeCount = -100;
let zero = 0;

let hexa = 0xFF; // Hexadecimal literal (255 in decimal)
console.log(typeof hexa); // "number"
let color_hexa = 0x27F5E0; // Hexadecimal literal (255 in decimal)

let octal = 0o755; // Octal literal (493 in decimal)

let story_harish = "";