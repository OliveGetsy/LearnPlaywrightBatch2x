let no_audi_olive_has = null; // This variable is declared and assigned the value null, meaning it intentionally has no value.
let pramod_has_two_cars;  // This variable is declared but not assigned a value, so it is undefined.

let age = 0; // This variable is declared and assigned the value 0, which is a number literal. It is not null or undefined.

let audi = ""; // This variable is declared and assigned an empty string, which is a string literal. It is not null or undefined.
console.log(typeof audi); // null
console.log(typeof age); // undefined